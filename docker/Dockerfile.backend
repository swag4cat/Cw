# Используем простой образ
FROM golang:1.21-alpine

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем только go.mod сначала
COPY go.mod .

# Скачиваем зависимости (их почти нет)
RUN go mod download

# Копируем остальной код
COPY . .

# Собираем приложение
RUN go build -o main .

# Экспонируем порт
EXPOSE 8080

# Команда для запуска
CMD ["./main"]
