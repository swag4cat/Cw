# 🍳 Кулинарная книга "KonKi"

Полноценное desktop-приложение для управления личной коллекцией рецептов с фото, избранным и поиском.

---

## ✨ Особенности

✅ Полная аутентификация - JWT токены с регистрацией и входом

✅ CRUD рецептов - Создание, чтение, обновление, удаление

✅ Загрузка фото - Поддержка изображений в формате Base64

✅ Система избранного - Закладки сохраняются между сессиями

✅ Умный поиск - По названию, описанию и ингредиентам

✅ Кроссплатформенность - Работает на Linux и Windows

✅ Графический интерфейс - Современный UI с эмодзи

✅ Docker-контейнеризация - Простое развёртывание

---

## 🏗️ Архитектура

Проект построен по клиент-серверной архитектуре:

```text
┌─────────────────┐     HTTP/JSON     ┌──────────────────┐
│   GUI Клиент    │◄─────────────────►│    Go Backend    │
│   (Fyne)        │     JWT токены    │    (REST API)    │
└─────────────────┘                   └─────────┬────────┘
      Linux/Windows                             │ PostgreSQL
                                                ▼
                                        ┌─────────────────┐
                                        │   PostgreSQL    │
                                        │   Database      │
                                        └─────────────────┘
```

---

## Технологический стек:

- Backend: Go 1.21+, PostgreSQL 15, JWT аутентификация
- Frontend: Fyne (кроссплатформенный GUI на Go)
- Инфраструктура: Docker, Docker Compose
- API: RESTful с Bearer токенами

---

## 🚀 Быстрый старт

**Предварительные требования**:
- Docker и Docker Compose
- Для GUI клиента в Docker: X11 сервер (на Linux)

**Запуск backend сервера**:

```bash
# Остановить если запущено
docker-compose down

# Собрать и запустить
docker-compose up --build backend postgres
```

Сервер будет доступен по адресу: http://localhost:8080

**Запуск GUI клиента**:

```bash
# Остановить если запущено
docker-compose -f docker-compose.client.yml down

# Собрать и запустить с правильными правами
UID=$(id -u) GID=$(id -g) docker-compose -f docker-compose.client.yml up --build client
```

---

## 🔧 Разработка и сборка

**Структура проекта**:

```text
coulinary_book 2.0/
├── backend/                      # Go сервер
│   ├── main.go                   # Точка входа
│   ├── auth/                     # JWT аутентификация
│   ├── models/                   # Структуры данных
│   ├── repository/               # Работа с БД
│   └── scripts/                  # SQL миграции
├── client/                       # GUI клиент
│   └── main.go                   # Весь код интерфейса
├── docker/                       # Dockerfile'ы
├── dist/                         # Собранные бинарники
├── docker-compose.yml            # Конфиг backend
├── docker-compose.client.yml     # Конфиг клиента
└── README.md                     # Этот файл
```

---

## Сборка бинарников

Для сборки standalone-версий клиента используйте Docker:

```bash
# Собрать бинарники
docker build -f Dockerfile.build -t culinary-builder .

docker run --rm -v $(pwd)/dist:/host-dist culinary-builder \
    sh -c "cp /app/dist/* /host-dist/"
```

---

## Запуск в Docker

Для разработки вся система запускается через Docker Compose:

**Backend**:

```bash
docker-compose up --build backend postgres
```

**Client (требует X11)**:

```bash
xhost +local:docker  # Разрешить Docker доступ к X11
UID=$(id -u) GID=$(id -g) docker-compose -f docker-compose.client.yml up --build client
```

---

## 🛠️ Устранение неполадок

**Проблемы с портами**:
Если порт 5432 (PostgreSQL) уже занят:

```bash
# Найти процесс, занимающий порт
sudo lsof -i :5432

# Если процесс найден, завершить его
sudo kill -9 <PID>

# Или завершить все процессы PostgreSQL
sudo pkill -f postgres
```

**Проблемы с GUI в Docker**:
Если клиент не запускается в Docker из-за проблем с отображением:

```bash
# Разрешить всем локальным клиентам доступ к X11
xhost +local:docker

# Или разрешить только конкретному контейнеру
xhost +local:`docker inspect --format='{{ .Config.Hostname }}' culinary_client`

# Проверить, что X11 сокет доступен
ls -la /tmp/.X11-unix/
```

**Проблемы с зависимостями**:
При сборке могут потребоваться дополнительные библиотеки:

```bash
# Для сборки на хосте (Ubuntu/Debian)
sudo apt-get install -y \
    libx11-dev libxcursor-dev libxrandr-dev \
    libxinerama-dev libxi-dev libxxf86vm-dev \
    mesa-common-dev libgl1-mesa-dev gcc-mingw-w64
```

---

## 📁 Распространение

**Готовые бинарники**:
В папке dist/ находятся готовые к запуску файлы:
- culinary-book-linux - для Linux (64-bit)
- culinary-book-windows.exe - для Windows (64-bit)

**Создание архивов для распространения**:

```bash
# Для Linux
tar -czf Кулинарная-Книга-Linux.tar.gz -C dist/ culinary-book-linux

# Для Windows
zip -j Кулинарная-Книга-Windows.zip dist/culinary-book-windows.exe
```

**Требования для пользователей**:
- Backend сервер должен быть запущен (docker-compose up backend postgres)
- Linux: бинарник требует стандартные библиотеки X11/OpenGL
- Windows: работает на Windows 7 и выше

---

## 📚 Технические детали

**База данных**:

Таблицы:

```sql
users (id, username, password_hash, email, created_at)
recipes (id, user_id, title, description, ingredients, instructions, 
         cooking_time, difficulty, image_base64, created_at, updated_at)
favorites (id, user_id, recipe_id, created_at)
```

Миграции находятся в backend/scripts/

**API Endpoints**:

```text
POST   /api/register          # Регистрация
POST   /api/login             # Вход
GET    /api/my-recipes        # Получить мои рецепты (требует токен)
POST   /api/create-recipe     # Создать рецепт (требует токен)
PUT    /api/update-recipe     # Обновить рецепт (требует токен)
DELETE /api/delete-recipe     # Удалить рецепт (требует токен)
GET    /api/favorites         # Получить избранное (требует токен)
POST   /api/favorites/add     # Добавить в избранное (требует токен)
DELETE /api/favorites/remove  # Удалить из избранного (требует токен)
GET    /api/health            # Проверка работоспособности
```

**Безопасность**:
- Пароли хэшируются с помощью bcrypt
- JWT токены на 24 часа
- Валидация входных данных на сервере
- SQL-инъекции предотвращаются использованием prepared statements

---

## 👨‍💻 Автор
🐙 GitHub: swag4cat
